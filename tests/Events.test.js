import Vue from 'vue'
import jsdom from 'jsdom';
import Events from '../src/compontents/Events.vue';

const renderer = require('vue-server-renderer').createRenderer();

describe('Events Test', ()=>{
    it('Test listing events', ()=>{

    })
})